version: '3'

services:
  documentcloud_frontend:
    image: node:12
    volumes:
      - nodemodules:/app/node_modules
      - .:/app
    working_dir: /app
    command: npm run serve
    networks:
      default:
        aliases:
          - www.dev.documentcloud.org
      squarelet_default:
        aliases:
          - www.dev.documentcloud.org
  
volumes:
  nodemodules:
    external: true

networks:
  squarelet_default:
    external: true