import type { Actions } from "./$types";
import type {
  Project,
  ProjectMembershipItem,
  ProjectUser,
} from "$lib/api/types";

export const actions = {
  async edit() {},

  async users() {},
} satisfies Actions;
